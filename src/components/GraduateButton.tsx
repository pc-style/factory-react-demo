import { CODE_DROID_URL } from '../factory/redirect';

export default function GraduateButton() {
  return (
    <a href={CODE_DROID_URL} className="cta">
      Try this on your code →
    </a>
  );
}
